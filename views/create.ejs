<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Hisaab - Khaatabook</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #1e1e2f; /* Dark background similar to ChatGPT */
      color: #ffffff; /* White text for readability */
    }
    .form-label {
      color: #ffffff; /* Ensure labels are white */
    }
    .form-control {
      background-color: #33334a; /* Darker input background */
      color: #ffffff; /* White text inside input */
      border: 1px solid #44445a; /* Subtle border for inputs */
    }
    .form-control::placeholder {
      color: #aaaaaa; /* Lighter placeholder text */
    }
    .btn {
      color: #ffffff;
    }
    .btn-primary {
      background-color: #007bff; /* Primary blue button */
    }
    .btn-secondary {
      background-color: #6c757d; /* Secondary muted gray button */
    }
    a {
      color: #ffffff; /* White links */
    }
    a:hover {
      color: #00bfff; /* Bright cyan hover */
    }
  </style>
</head>
<body>
  <div class="container my-4">
    <header class="d-flex justify-content-between align-items-center mb-4">
      <h1>Khaatabook</h1>
      <a href="/" class="btn btn-success btn-lg ">Home</a>
    </header>
    <h2>Create New Hisaab</h2>
    <form action="/createFile" method="post">
      <div class="mb-3">
        <label for="title" class="form-label mb-3 mt-3">Hisaab Title</label>
        <input type="text" id="title" name="title" class="form-control w-75" placeholder="Shopping Hisaab, Ghar ka Kharch..." required>
      </div>
      <div class="mb-3 mt-3">
        <label for="description" class="form-label  mb-3 mt-3">Description</label>
        <textarea id="description" name="description" rows="8" class="form-control w-75  mb-3" placeholder="Daal, Aata, Cheeni" rows="4" required></textarea>
      </div>
      <button type="submit" class="btn btn-primary btn-lg  mb-3 mt-3">Create New Hisaab</button>
    </form>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
