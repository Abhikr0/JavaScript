<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Hisaab - Khaatabook</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #1e1e2f; /* Dark background similar to ChatGPT */
      color: #ffffff; /* White text for contrast */
    }
    .btn {
      color: #ffffff;
    }
    .btn-success {
      background-color: #4caf50; /* Slightly muted green for the Home button */
    }
    .btn-primary {
      background-color: #007bff; /* Standard blue for primary buttons */
    }
    .card {
      background-color: #33334a; /* Darker card background */
      border: none;
    }
    .card-header {
      background-color: #44445a; /* Slightly lighter header background */
    }
    a {
      color: #ffffff; /* White links */
    }
    a:hover {
      color: #00bfff; /* Bright cyan hover effect */
    }
  </style>
</head>
<body>
  <div class="container my-4">
    <header class="d-flex justify-content-between align-items-center mb-4">
      <h1>Khaatabook</h1>
      <a href="/" class="btn btn-success btn-lg">Home</a>
    </header>
    <h2 class="mb-3 mt-4">View Hisaab</h2>
    <p class="mb-3 mt-4"><strong>Hisaab of <%= filename %> date</strong></p>
    
    <div class="card text-white mb-3" style="max-width: 50rem;">
        <div class="card-header">Your Hisaab :</div>
        <div class="card-body">
          <h5 class="card-title"><%= filename %></h5>
          <p class="card-text"><%= data %></p>
        </div>
      </div>
    <a href="/edit/<%=filename%>" class="btn btn-primary btn-lg btn-rounded">Edit Hisaab</a>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
